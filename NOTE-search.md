# solver/search 分支

目标：为 Pipes 棋盘实现**搜索式求解器**（区别于默认纯规则推理模式）。

当前实现：`solver.py --search` 在 R1–R9 不动点后执行 DFS + 约束传播，
支持 `first`、`mrv`、`degree`、`edge` 四种分支策略；`--compare` 输出节点数、
回溯数、深度和耗时，便于在不同尺寸题面上基准测试。已支持普通与 **wrap
（环形穿墙）** 两种棋盘，wrap 由题面文件里的 `wrap:` 字段自动识别。

思路：
- 在规则推理（R1-R9）做不动点传播后，若仍有未确定格子，用回溯/DFS + 约束传播（类似 DPLL）选分歧点枚举。
- 约束：最终解是一棵生成树（全连通、无环、V-1 条连接边）。
- 目标是能解出规则推理解不出的题，并能输出与规则推理一致的推导日志以便 gui 回放。

## wrap（环形）模式

网站首页把 wrap 题挂在 `?size=10..16`（普通尺寸 `0..6`，`+10` 即 wrap；
`7/8/9` 与 `17/18/19` 是 Special 每日/周/月赛，尺寸由服务器定）。页面配置里
`wrap:1` 标明是环形。抓题：`python get_puzzle.py wrap 10`（或 `10x10-wrap`、
直接给 size 序号 `13`），保存的 txt 里带 `wrap: 1`。

**几何**：环面上棋盘左右相接、上下相接（torus），没有"棋盘外"。
`solver.py` 里 `Solver(wrap=True)` 时 `has_edge` 恒真、边键与两端坐标一律取模，
新增 `all_edges()` 统一枚举盘面上的边（含首尾相接的）。

**开局难点**：wrap 没有边界墙，R1 一条也推不动，规则传播在开局**零推导**，
必须靠搜索挑分歧点。`mrv`（候选最少的格子优先）开局会先落到直线型（剩 2 种）
或十字型（剩 1 种）的格子上，比 `first`（按行优先）收敛快得多。

**规则适配**：R5–R8 的生成树性质在环面上同样成立（V 格仍需 V-1 条连接、
无环、全连通），另加 wrap 专属：

- **R9 环必须断开**：环面上每行的 w 条横边首尾相接成环、每列的 h 条竖边同理，
  生成树不能整圈全取连接 → 某行/列只剩一条未定而其余全连接时，该边必是墙。

盘面校验与官方 md5 判定在 wrap 下也照常工作（`board_check(wrap=True)` 的
连通洪泛按环面取模；官方胜利判定仍是 `md5(task + 旋转串)`，与是否 wrap 无关）。

**实测**：4/5/7/10/15/20/25 wrap 多道随机题全部搜索解出且官方 md5 通过，
连接边恰为 V-1 条且无环（见 `test_wrap.py`）；25x25 wrap 约 4s，其余更快。
