# solver/search 分支

目标：为 Pipes 棋盘实现**搜索式求解器**（区别于 main 上纯规则推理的 solver.py）。

思路：
- 在规则推理（R1-R8）做不动点传播后，若仍有未确定格子，用回溯/DFS + 约束传播（类似 DPLL）选分歧点枚举。
- 约束：最终解是一棵生成树（全连通、无环、V-1 条连接边）。
- 目标是能解出 main 上规则推理解不出的题，并能输出与规则推理一致的推导日志以便 gui 回放。
